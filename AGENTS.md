# Project rules

- 作業は `PLAN.md` のPhase単位で進める。
- 大きな仕様変更、依存追加、エンジン・モデル更新は事前承認を得る。
- 棋譜・局面・解析結果を外部送信しない。
- `public/engine/` と `public/models/` の生成物を手作業で差し替えない。出典、ビルドコマンド、SHA-256を記録する。
- 変更範囲に対応する最小テストから実行する。
- commit、push、公開はユーザーの明示承認後に行う。
